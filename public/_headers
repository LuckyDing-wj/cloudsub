# Vite emits content-hashed files under /assets/ — safe to cache for a year.
# The root index.html keeps the default CAAS-friendly revalidation headers.
/assets/*
  Cache-Control: public, max-age=31536000, immutable